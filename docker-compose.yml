services:
  mongo:
    image: mongo:7
    container_name: ems_asset_registry_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ems_asset_registry
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
